import { FiterbuyerPipe } from './fiterbuyer.pipe';

describe('FiterbuyerPipe', () => {
  it('create an instance', () => {
    const pipe = new FiterbuyerPipe();
    expect(pipe).toBeTruthy();
  });
});
